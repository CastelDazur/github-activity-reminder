name: Daily GitHub Activity Reminder

on:
  schedule:
    # Запускается каждый день в 09:00 UTC (10:00 CET)
    - cron: '0 9 * * *'
  workflow_dispatch: # Позволяет запускать вручную

jobs:
  create-reminder:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: read
    
    steps:
      - name: Create Daily Reminder Issue
        uses: actions/github-script@v7
        with:
          script: |
            const today = new Date().toISOString().split('T')[0];
            const dayOfWeek = new Date().toLocaleDateString('ru-RU', { weekday: 'long' });
            
            // Список задач на день
            const dailyTasks = [
              '✅ Сделать хотя бы 1 коммит в любой проект',
              '📝 Обновить документацию (README, комментарии)',
              '🐛 Исправить bug или добавить feature',
              '💡 Написать код для нового функционала',
              '🔄 Сделать code review старого кода'
            ];
            
            // Идеи для активности
            const activityIdeas = [
              '**DARY Platform**: добавить новую функцию или исправить баг',
              '**Telegram Monitor**: улучшить алгоритмы фильтрации',
              '**Business Template**: оптимизация производительности',
              'Создать новый utility проект или npm пакет',
              'Внести вклад в open-source проект',
              'Написать техническую статью в GitHub Gist',
              'Обновить профиль README с новыми достижениями'
            ];
            
            // Случайная мотивационная цитата
            const quotes = [
              '"Код - это поэзия логики" 💻',
              '"Каждый коммит - шаг к мастерству" 🚀',
              '"Лучший способ предсказать будущее - создать его" 🌟',
              '"Постоянство важнее совершенства" 📈',
              '"Программирование - это искусство решения проблем" 🎨'
            ];
            
            const randomQuote = quotes[Math.floor(Math.random() * quotes.length)];
            const randomIdea = activityIdeas[Math.floor(Math.random() * activityIdeas.length)];
            
            // Создаем issue
            const issueBody = `## 🌅 Доброе утро! Время для активности на GitHub
            
### 📅 ${dayOfWeek}, ${today}

${randomQuote}

---

### ✨ Сегодняшние задачи:

${dailyTasks.map(task => `- ${task}`).join('\n')}

---

### 💡 Идея на сегодня:

${randomIdea}

---

### 📊 Отслеживание:

- [ ] Сделан хотя бы 1 коммит
- [ ] Обновлена документация
- [ ] Закрыт этот issue после выполнения

---

### 🎯 Цель: Поддерживать активный GitHub профиль

**Помни**: Даже небольшой вклад важен! Consistency > Perfection 🔥

_Этот issue был создан автоматически для напоминания о ежедневной активности._`;

            try {
              const response = await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: `📅 Напоминание: GitHub Activity - ${today}`,
                body: issueBody,
                labels: ['reminder', 'daily-activity']
              });
              
              console.log(`Issue создан: ${response.data.html_url}`);
            } catch (error) {
              console.error('Ошибка при создании issue:', error);
              throw error;
            }
